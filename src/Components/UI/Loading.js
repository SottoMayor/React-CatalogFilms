import React from 'react'
import { StyledLoading } from '../../styled-components/Styles'

const Loading = () => {
    return (
        <StyledLoading>
            <div className="loader">Loading...</div>
        </StyledLoading>
    )
}

export default Loading
